import LoginForm from '@/components/LoginForm';
import React from 'react';

const LoginPage: React.FC = () => {
  // Placeholder for login form component
  return (
    <div>
      <h1>Login Page</h1>
      <LoginForm />
    </div>
  );
};

export default LoginPage;
